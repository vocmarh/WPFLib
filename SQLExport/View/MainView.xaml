<Window x:Class="SQLExport.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SQLExport.View"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="500">
    <Grid>
        <StackPanel>
            <Button
                Command="{Binding CreateDataTable}"
                Content="Создать SQL таблицу"
                Margin="10"
                Padding="10"/>

            <Button
                Command="{Binding ExportDataTable}"
                Content="Экспортировать базу данных в SQL"
                Margin="10"
                Padding="10"/>
        </StackPanel>
        
    </Grid>
</Window>
